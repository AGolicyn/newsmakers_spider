name: Runner
on:
  schedule:
    - cron: '4 * * * *'
jobs:
  test_shedule:
    runs-on: ubuntu-latest
    steps:
      -
        name: Calling the spider
        run: echo 'Time to crowl :)'
        
